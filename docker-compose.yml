services:
  maia-chess:
    build: .
    container_name: maia_chess_app
    restart: unless-stopped
    tty: true
    stdin_open: true
    ports:
      - "5000:5000"
    environment:
      - PYTHONUNBUFFERED=1
    restart: always