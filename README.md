# ML Dead Chess Champions


## Installation

Python 3.10.19 is used for this project, as it is incompatible with newer versions due to specific Maia-2 dependencies.

### Dependencies

To install dependencies, run:

```bash
pip install -r requirements.txt
```

### Stockfish

You should install the Stockfish model and place the executable into the `stockfish_model` folder, naming it `stockfish`. Alternatively, you can modify the Stockfish path in `src/core/config.py` by updating the `STOCKFISH_MODEL_PATH` variable.

## Use of AI in the project
All comments in this repository were generated by **Github Copilot**.

The `logging` and `HEADERS` in `src/fetch_games.py` were generated by **Gemini**.

The README was largely corrected by **Github Copilot** and **Gemini**.

Commit messages were generated by **Github Copilot**.

The file `src/utils_plot.py` was entirely generated by **Gemini**.

The same applies to the files `src/app.py`, `src/engine.py`, `src/static/script.js` and `src/templates/index.html`.