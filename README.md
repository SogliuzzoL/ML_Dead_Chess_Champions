# ML Dead Chess Champions


## Installation

Pour installer les dépendances, exécutez :

```bash
pip install -r requirements.txt
```

## Utilisation de l'IA dans le projet
Tous les commentaires de ce dépôt ont été générés par **Github Copilot**.

Le `logging` ainsi que le `HEADERS` dans `src/fetch_games.py` est généré par **Gemini**.

Le README est en grande partie généré par **Github Copilot**.

Les messages de commit ont été généré par **Github Copilot**.

Le fichier `src/utils_plot.py` est entierement généré par **Gemini**.

De même pour les fichiers `src/app.py`, `src/engine.py`, `src/static/script.js` et `src/templates/index.html`.